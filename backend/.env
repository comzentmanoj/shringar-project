

# Copy this file to ".env" and fill in real values. Never commit ".env" itself.

# Server
PORT=5000

# MongoDB connection string.
# Local MongoDB:      mongodb://127.0.0.1:27017/shringar
# MongoDB Atlas (free tier): get this from atlas.mongodb.com after creating a cluster
MONGO_URI="mongodb+srv://shrinagar:shrinagar2026@cluster0.sk4mybp.mongodb.net/shringar?appName=Cluster0"

# JWT secret - use a long random string in production
# generate one with: node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"
JWT_SECRET=f54004791c1d2f9cfc6abacc8fed62887c4077626a24b20e2f3069bda6bb37a30581e6dee6eb01558c02131638dcda8a
JWT_EXPIRES_IN=7d

# First admin account, created by "npm run seed:admin"
ADMIN_EMAIL=admin@shringarbylakshuu.com
ADMIN_PASSWORD=shrinagar2026!

# Comma-separated list of allowed frontend origins for CORS
CLIENT_ORIGIN=http://localhost:4200